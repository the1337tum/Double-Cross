/**
 * Use structs for the names of your files
 * so that I can link to them with the parser.
**/

extern static struct LevelFiles{
    char *config = "Config.txt";
    /**
     * Can you give me some more examples, 
     * alturnatively, just add the names of all 
     * like the example above.
    **/
} LEVEL_FILES;

